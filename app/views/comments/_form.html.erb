<%= form_with(model: [review, comment], class: "comment_form") do |f| %>
  <%= f.text_field :text, class: "comment_text", placeholder: "コメント", size: "60" %>
  <%= f.submit "投稿", class: "comment-submit" %>
<% end %>